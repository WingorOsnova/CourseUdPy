GAME1:
Клавиши

⬆️ ⬇️ ⬅️ ➡️ — управление

P — пауза

R — перезапуск после Game Over

Esc — выход

GAME2:
Что внутри (фишки)

4 режима:

CLASSIC: стены убивают.

WRAP: выход за край — появляешься с противоположной стороны.

OBSTACLES: процедурные препятствия (кольца/крест/рандом).

MARATHON: постепенное ускорение после каждых нескольких очков.

Бусты/предметы:

food (+1 очко, +1 рост), всегда спавнится.

gold (+5 очков, +3 рост, исчезает через ~6с).

slow (замедляет время на 5с).

ghost (режим призрака — игнор самоукус на 8с).

shrink (сокращает хвост на 3 сегмента).

reverse (инверсия управления на 6с).

Меню и настройки: выбор режима (1–4), старт (Space/Enter), настройки (Tab), изменение скорости шага и размера сетки, сохранение в snake_data.json.

HUD: очки, лучший счёт по режиму, текущая скорость шагов/сек, активные эффекты (⏳👻🔁).

Пауза/перезапуск/фуллскрин: P — пауза, R — рестарт, F — полноэкранный.

Частицы/анимация: всплеск частиц при поедании предметов, градиент тела, глаза у головы 🐍.

Сохранение рекордов по режимам в snake_data.json.

Подсказки

Если окно слишком маленькое/большое — потяни границы (подстраивается tile) или зайди в Settings (Tab) и поменяй сетку/скорость шага.

На macOS, если клавиатура «липнет» в полноэкранном, переключись F обратно.

Хочешь ещё жести — добавим уровни с портальными входами, систему жизней, таблицу лидеров, геймпад, звук, скины змейки или редактор карт. Скажи, что именно качнуть следующим шагом. 🎮🐍